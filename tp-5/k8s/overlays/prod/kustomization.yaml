apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: app-prod

resources:
- ../../base

patches:
- path: patch-configmap.yaml
- path: patch-deployment.yaml

commonLabels:
  environment: prod
